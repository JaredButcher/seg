{
  "name": "@seg/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Authoritative game server: HTTP, auth, persistence, lobby, and match hosting.",
  "//exports": "Subpaths only, and deliberately no '.': the package root is src/index.ts, which starts a listening server as a side effect of being imported. @seg/tools reaches the match subsystem through './match' to benchmark the publish path (planning/17 §9); that subtree imports @seg/shared and nothing else, so no database or socket comes with it.",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@node-rs/argon2": "^2.0.2",
    "@seg/shared": "workspace:*",
    "better-sqlite3": "^13.0.3",
    "tsx": "^4.20.6",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/ws": "^8.18.1"
  }
}
