/**
 * @seg/shared/math — the deterministic primitives everything else is built on.
 *
 * Vector and angle helpers join the PRNG here as the simulation lands (planning/10 §1).
 */

export * from './rng.js';
