/**
 * @seg/shared/math — the deterministic primitives everything else is built on.
 *
 * Vector and angle helpers join the PRNG and the decibel arithmetic here as the simulation
 * lands (planning/10 §1).
 */

export * from './decibels.js';
export * from './rng.js';
