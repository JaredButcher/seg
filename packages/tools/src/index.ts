/**
 * @seg/tools — development tooling. Nothing here ships to players.
 *
 * Planned contents (planning/10 §1):
 *   scenario/         the scenario DSL, shared by tests, balance, and the practice range
 *   balance-matrix    detection-range matrices, per region archetype
 *   map-gallery       render a grid of generated maps for human review
 *   map-inspect       one seed: sectors, portals, navmesh, invariant report
 *   replay            headless replay + determinism check
 *   loadtest          synthetic clients against a running server
 *   bench-*           acoustics, tick, mapgen, bandwidth, navigation
 */

export const TOOLS_PLACEHOLDER = true;
